---
phase: 01-foundation-libraries
plan: 04
type: tdd
wave: 2
depends_on: ["01-02"]
files_modified:
  - lib/bulk_prompter.py
  - lib/tag_generator.py
  - word_banks/adjectives.txt
  - word_banks/styles.txt
  - word_banks/moods.txt
  - word_banks/scene_details.txt
  - tests/test_bulk_prompter.py
  - tests/test_tag_generator.py
  - lib/__init__.py
autonomous: true
requirements:
  - LIB-07
  - LIB-08
  - LIB-15

must_haves:
  truths:
    - "Bulk prompter generates N distinct prompt variants from a base prompt"
    - "Bulk prompter supports all 6 mutation strategies: synonym_swap, detail_injection, style_shuffle, weight_jitter, reorder, template_fill"
    - "Bulk prompter loads word banks from shipped .txt files"
    - "Tag generator extracts tags from prompt text (Layer 1)"
    - "Tag generator looks up tags from topic bank dict (Layer 2)"
    - "Tag generator calls LLM API for tag generation and handles failure gracefully (Layer 3)"
    - "Generated prompt variants are distinct from each other"
  artifacts:
    - path: "lib/bulk_prompter.py"
      provides: "6 mutation strategies and variant generation"
      exports: ["generate_variants", "STRATEGIES"]
      min_lines: 100
    - path: "lib/tag_generator.py"
      provides: "3-layer tag pipeline"
      exports: ["generate_tags", "extract_tags_from_prompt"]
      min_lines: 60
    - path: "word_banks/adjectives.txt"
      provides: "Adjective word bank for mutations"
      min_lines: 20
    - path: "word_banks/styles.txt"
      provides: "Style word bank for mutations"
      min_lines: 20
    - path: "word_banks/moods.txt"
      provides: "Mood word bank for mutations"
      min_lines: 20
    - path: "word_banks/scene_details.txt"
      provides: "Scene detail word bank for mutations"
      min_lines: 20
    - path: "tests/test_bulk_prompter.py"
      provides: "Bulk prompter tests"
      min_lines: 60
    - path: "tests/test_tag_generator.py"
      provides: "Tag generator tests"
      min_lines: 40
  key_links:
    - from: "lib/bulk_prompter.py"
      to: "word_banks/*.txt"
      via: "load_word_bank reads .txt files relative to package"
      pattern: "Path.*word_banks"
    - from: "lib/tag_generator.py"
      to: "lib/response_parser.py"
      via: "extract_value for LLM response parsing"
      pattern: "from .response_parser import"
---

<objective>
Create the bulk prompter (6 mutation strategies with word banks) and tag generator (3-layer pipeline) modules. Bulk prompter ships with curated word bank files. Tag generator depends on response_parser from Plan 02 for LLM response handling.

Purpose: These modules power the Pipeline Controller's prompt variant generation and tag generation in Phase 3. Bulk Prompter also backs the standalone Bulk Prompter node in Phase 2.
Output: lib/bulk_prompter.py, lib/tag_generator.py, word_banks/*.txt with test suites.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-libraries/01-RESEARCH.md
@.planning/phases/01-foundation-libraries/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create word banks and bulk_prompter module with TDD</name>
  <files>
    word_banks/adjectives.txt
    word_banks/styles.txt
    word_banks/moods.txt
    word_banks/scene_details.txt
    lib/bulk_prompter.py
    tests/test_bulk_prompter.py
    lib/__init__.py
  </files>
  <action>
1. Create word bank files (minimal but functional, ~25-30 entries each per research recommendation):
   - `word_banks/adjectives.txt`: One word per line. Include: beautiful, stunning, dramatic, ethereal, vibrant, delicate, majestic, serene, mysterious, radiant, luminous, atmospheric, intricate, bold, subtle, vivid, moody, dreamlike, cinematic, organic, elegant, raw, dynamic, soft, fierce
   - `word_banks/styles.txt`: One phrase per line. Include: photorealistic, oil painting, watercolor, digital art, concept art, anime style, studio photography, film noir, impressionist, surrealist, art nouveau, minimalist, hyperrealistic, cel shaded, cyberpunk, steampunk, gothic, baroque, pop art, vaporwave, low poly, pixel art, sketch, charcoal drawing, ukiyo-e
   - `word_banks/moods.txt`: One word per line. Include: serene, melancholic, joyful, mysterious, ominous, peaceful, chaotic, nostalgic, hopeful, eerie, triumphant, contemplative, whimsical, intense, tranquil, foreboding, euphoric, somber, playful, haunting
   - `word_banks/scene_details.txt`: One phrase per line. Include: golden hour lighting, morning mist, rain droplets, lens flare, volumetric fog, bokeh background, dramatic shadows, dappled sunlight, neon reflections, dust particles, starry sky, autumn leaves, cracked earth, flowing water, snow falling, firefly lights, cobblestone path, overgrown ruins, crystal formations, smoke wisps, light rays through clouds, wet surfaces, frost patterns, wildflowers

2. RED: Create `tests/test_bulk_prompter.py` with failing tests:
   - generate_variants returns exactly N variants
   - All N variants are distinct strings (no duplicates)
   - Each variant is different from the base prompt
   - synonym_swap strategy replaces at least one adjective-like word
   - detail_injection strategy appends scene detail text
   - style_shuffle strategy adds or changes style modifier
   - weight_jitter strategy adds or modifies (word:weight) syntax in the 1.0-1.4 range
   - reorder strategy changes clause order (for prompts with commas)
   - template_fill strategy fills {mood}, {detail}, {style} wildcards
   - Word banks load successfully (not empty)
   - Requesting specific strategies via list parameter works (e.g., strategies=["synonym_swap", "reorder"])
   - Works with very short prompts (single word)
   - Works with long prompts (10+ clauses)
   - Seeded random produces reproducible results (pass seed parameter)

3. GREEN: Create `lib/bulk_prompter.py` implementing:
   - `STRATEGIES` list: ["synonym_swap", "detail_injection", "style_shuffle", "weight_jitter", "reorder", "template_fill"]
   - `_load_word_bank(name: str) -> list[str]`: Loads from word_banks/{name}.txt relative to this file's location using `Path(__file__).parent.parent / "word_banks" / f"{name}.txt"`. Strips whitespace, ignores empty lines.
   - `generate_variants(base_prompt: str, num_variants: int = 10, strategies: Optional[list[str]] = None, seed: Optional[int] = None) -> list[str]`
     - If strategies is None, use all 6
     - For each variant, randomly pick a strategy from the allowed list and apply it
     - Ensure uniqueness: if a generated variant duplicates, retry with different strategy/random state
     - Uses random.Random(seed) for reproducibility when seed is provided
   - Strategy implementations:
     - `_synonym_swap(prompt, rng)`: Split by comma, pick a clause, replace an adjective from adjectives bank
     - `_detail_injection(prompt, rng)`: Append a random scene_detail
     - `_style_shuffle(prompt, rng)`: Append or replace a style from styles bank
     - `_weight_jitter(prompt, rng)`: Pick a word, wrap in (word:weight) with weight between 1.0-1.4
     - `_reorder(prompt, rng)`: Split by comma, shuffle clauses
     - `_template_fill(prompt, rng)`: Replace {mood}, {detail}, {style} placeholders with bank entries
   - All file operations use pathlib.Path

4. Run tests, all must pass. Refactor if needed.

5. Update `lib/__init__.py` to export: `from .bulk_prompter import generate_variants, STRATEGIES`
  </action>
  <verify>
    <automated>cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_bulk_prompter.py -v</automated>
  </verify>
  <done>Bulk prompter generates N distinct variants using 6 strategies, word banks load from shipped .txt files, seeded random is reproducible, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create tag_generator module with TDD</name>
  <files>
    lib/tag_generator.py
    tests/test_tag_generator.py
    lib/__init__.py
  </files>
  <action>
1. RED: Create `tests/test_tag_generator.py` with failing tests:

   Layer 1 (prompt extraction) tests:
   - extract_tags_from_prompt("a golden sunset over calm ocean waves, cinematic lighting") returns tags including "sunset", "ocean", "waves", "cinematic"
   - Filler words stripped: "a", "the", "over", "with", "and", "of", "in", "on", "at", "to", "for", "is", "are", "was", "were"
   - Returns list of strings, all lowercase
   - Handles empty prompt gracefully (returns empty list)
   - Handles prompt with no commas (single clause)

   Layer 2 (topic bank lookup) tests:
   - lookup_topic_tags("sunset_beach", {"sunset_beach": {"content": ["beach"], "mood": ["warm"]}}) returns categorized dict
   - Missing topic returns empty dict
   - None topic_bank returns empty dict

   Layer 3 (LLM generation) tests:
   - generate_tags with LLM disabled returns only Layer 1 + 2 results
   - generate_tags with mock LLM call merges all 3 layers
   - LLM failure falls back gracefully to Layer 1 + 2 (no crash)
   - Returned tags are categorized into content, style, mood, technical

   Full pipeline tests:
   - generate_tags returns dict with keys: content, style, mood, technical
   - generate_tags returns tag_sources dict showing which layer produced each tag
   - Technical tags include resolution string
   - Duplicate tags across layers are deduplicated

2. GREEN: Create `lib/tag_generator.py` implementing:
   - `extract_tags_from_prompt(prompt: str) -> list[str]` (Layer 1)
     - Split by comma, strip each part, split by space
     - Remove filler words (FILLER_WORDS constant)
     - Lowercase, deduplicate
   - `lookup_topic_tags(topic: str, topic_bank: Optional[dict]) -> dict` (Layer 2)
     - Lookup topic in bank, return categorized tags or empty dict
   - `generate_llm_tags(topic: str, prompt: str, llm_config: Optional[dict]) -> dict` (Layer 3)
     - If llm_config is None or empty, return empty dict
     - Build request to LLM API asking for categorized tags
     - Use response_parser.extract_value to parse response
     - On any exception, log warning and return empty dict (graceful fallback)
   - `generate_tags(prompt: str, topic: str, resolution: str = "", topic_bank: Optional[dict] = None, llm_config: Optional[dict] = None) -> tuple[dict, dict]`
     - Runs all 3 layers
     - Merges results into categorized tags dict
     - Builds tag_sources dict tracking provenance
     - Adds resolution to technical tags
     - Deduplicates within each category
     - Returns (tags_dict, tag_sources_dict)
   - Import: `from .response_parser import extract_value, strip_markdown_code_blocks`

3. Run tests, all must pass. Refactor if needed.

4. Update `lib/__init__.py` to export: `from .tag_generator import generate_tags, extract_tags_from_prompt`
  </action>
  <verify>
    <automated>cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_tag_generator.py -v</automated>
  </verify>
  <done>Tag generator runs 3-layer pipeline: prompt extraction, topic bank lookup, LLM generation. Graceful fallback on LLM failure. Tags are categorized and deduplicated. All tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite for both modules:
```bash
cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_bulk_prompter.py tests/test_tag_generator.py -v
```
Verify word bank files exist and have content:
```bash
wc -l word_banks/*.txt
```
Verify tag_generator imports from response_parser:
```bash
grep "from .response_parser import" lib/tag_generator.py
```
</verification>

<success_criteria>
- Bulk prompter generates N distinct variants using all 6 mutation strategies
- Word banks ship with 20+ entries each
- Tag generator runs 3-layer pipeline with graceful LLM fallback
- Tags are categorized (content, style, mood, technical) with provenance tracking
- All tests pass (pytest exit code 0)
- No os.path usage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-libraries/01-04-SUMMARY.md`
</output>
