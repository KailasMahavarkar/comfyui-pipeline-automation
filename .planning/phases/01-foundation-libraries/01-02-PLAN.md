---
phase: 01-foundation-libraries
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/response_parser.py
  - lib/fingerprint.py
  - tests/test_response_parser.py
  - tests/test_fingerprint.py
  - lib/__init__.py
autonomous: true
requirements:
  - LIB-06
  - LIB-12
  - LIB-13
  - LIB-15

must_haves:
  truths:
    - "Response parser extracts values from nested JSON using dot-path notation"
    - "Response parser auto-parses stringified JSON inside response fields"
    - "Response parser strips markdown code blocks from LLM responses before parsing"
    - "Fingerprint module computes identical SHA-256 hashes for the same workflow regardless of dict insertion order"
    - "Fingerprint module computes different hashes when node connections or models change"
    - "Fingerprint module saves, loads, and compares fingerprints with actionable collision error messages"
  artifacts:
    - path: "lib/response_parser.py"
      provides: "Dot-path JSON extraction with auto-parse and markdown stripping"
      exports: ["extract_value", "strip_markdown_code_blocks"]
      min_lines: 30
    - path: "lib/fingerprint.py"
      provides: "Workflow fingerprint computation, save/load/compare"
      exports: ["compute_fingerprint", "save_fingerprint", "load_fingerprint", "compare_fingerprints"]
      min_lines: 60
    - path: "tests/test_response_parser.py"
      provides: "Response parser tests"
      min_lines: 40
    - path: "tests/test_fingerprint.py"
      provides: "Fingerprint module tests"
      min_lines: 50
  key_links:
    - from: "lib/fingerprint.py"
      to: "hashlib.sha256"
      via: "canonical JSON serialization then hash"
      pattern: "hashlib\\.sha256"
    - from: "lib/fingerprint.py"
      to: "json.dumps"
      via: "sort_keys=True for deterministic serialization"
      pattern: "sort_keys=True"
    - from: "lib/response_parser.py"
      to: "json.loads"
      via: "auto-parse stringified JSON during dot-path walk"
      pattern: "json\\.loads"
---

<objective>
Create the response parser (dot-path JSON extraction) and fingerprint (workflow identity) library modules. Both have well-defined input/output contracts ideal for TDD.

Purpose: Response parser is needed by tag_generator (Plan 04). Fingerprint module is needed by Pipeline Controller (Phase 3). Both are pure computation with no external dependencies.
Output: lib/response_parser.py, lib/fingerprint.py with full test suites.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-libraries/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create response_parser module with TDD</name>
  <files>
    lib/response_parser.py
    tests/test_response_parser.py
    lib/__init__.py
  </files>
  <action>
1. RED: Create `tests/test_response_parser.py` with failing tests:
   - Simple dot-path: extract_value({"a": {"b": "c"}}, "a.b") -> "c"
   - Array index: extract_value({"choices": [{"text": "hi"}]}, "choices.0.text") -> "hi"
   - OpenAI-style nested: extract_value({"choices": [{"message": {"content": "result"}}]}, "choices.0.message.content") -> "result"
   - Auto-parse stringified JSON: extract_value({"data": '{"prompt": "hello"}'}, "data.prompt") -> "hello"
   - Markdown code block stripping: strip_markdown_code_blocks('```json\n{"a":1}\n```') -> '{"a":1}'
   - Markdown without language tag: strip_markdown_code_blocks('```\n{"a":1}\n```') -> '{"a":1}'
   - No code blocks passes through: strip_markdown_code_blocks('{"a":1}') -> '{"a":1}'
   - Combined: stringified JSON wrapped in markdown code blocks inside a response field
   - Missing path returns None (not KeyError)
   - Empty path returns the full data object
   - Deeply nested path (5+ levels)
   - Path to non-existent key returns None

2. GREEN: Create `lib/response_parser.py` implementing:
   - `strip_markdown_code_blocks(text: str) -> str` using regex: r"```(?:json)?\s*\n?(.*?)\n?\s*```" with re.DOTALL
   - `extract_value(data: dict, dot_path: str) -> Any` that walks nested JSON using dot-path notation
   - During walk, if current value is a string, attempt json.loads(strip_markdown_code_blocks(current)) before continuing
   - Handle list indexing when part.isdigit()
   - Return None for missing keys (never raise)
   - Uses pathlib import (LIB-15 compliance even though this module doesn't do file I/O)

3. Run tests, all must pass. Refactor if needed.

4. Update `lib/__init__.py` to export: `from .response_parser import extract_value, strip_markdown_code_blocks`
  </action>
  <verify>
    <automated>cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_response_parser.py -v</automated>
  </verify>
  <done>extract_value handles nested dicts, arrays, stringified JSON, and markdown-wrapped responses; returns None for missing paths; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create fingerprint module with TDD</name>
  <files>
    lib/fingerprint.py
    tests/test_fingerprint.py
    lib/__init__.py
  </files>
  <action>
1. RED: Create `tests/test_fingerprint.py` with failing tests:
   - compute_fingerprint returns a 64-char hex string (SHA-256)
   - Same inputs in different order produce identical hash (deterministic)
   - Different node_types produce different hash
   - Different connections produce different hash
   - Different checkpoints produce different hash
   - Different loras produce different hash
   - Different workflow_name produces different hash
   - save_fingerprint writes JSON file to given path
   - load_fingerprint reads back saved fingerprint
   - compare_fingerprints returns (True, None) for matching fingerprints
   - compare_fingerprints returns (False, error_message) for mismatching fingerprints
   - Error message includes diff details: which fields changed (e.g., "Checkpoint changed: model_a.safetensors -> model_b.safetensors")
   - Error message includes actionable options (choose different name, delete folder, reset_workflow)
   - Fingerprint file contains metadata: hash, node_types, checkpoints, loras, workflow_name, created_at
   - load_fingerprint on missing file returns None
   - All file operations use pathlib.Path

2. GREEN: Create `lib/fingerprint.py` implementing:
   - `compute_fingerprint(node_types: list[str], connections: list[tuple[str, str]], checkpoints: list[str], loras: list[str], workflow_name: str) -> str`
     - Build canonical dict with sorted lists and sorted connection strings ("src->dst")
     - json.dumps(canonical, sort_keys=True, separators=(",", ":"))
     - hashlib.sha256(serialized.encode("utf-8")).hexdigest()
   - `save_fingerprint(filepath: Path, fingerprint: str, node_types: list[str], checkpoints: list[str], loras: list[str], workflow_name: str) -> None`
     - Writes JSON with hash, metadata, and created_at timestamp
     - filepath.parent.mkdir(parents=True, exist_ok=True)
   - `load_fingerprint(filepath: Path) -> Optional[dict]`
     - Returns None if file doesn't exist
     - Returns full dict including hash and metadata
   - `compare_fingerprints(current_hash: str, current_meta: dict, saved: dict) -> tuple[bool, Optional[str]]`
     - Returns (True, None) if hashes match
     - Returns (False, error_string) with detailed diff and actionable options if mismatch

3. Run tests, all must pass. Refactor if needed.

4. Update `lib/__init__.py` to also export fingerprint functions.
  </action>
  <verify>
    <automated>cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_fingerprint.py -v</automated>
  </verify>
  <done>Fingerprint produces deterministic hashes, saves/loads/compares correctly, provides actionable collision error messages, all tests pass</done>
</task>

</tasks>

<verification>
Run full test suite for both modules:
```bash
cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_response_parser.py tests/test_fingerprint.py -v
```
Verify no os.path imports:
```bash
grep -r "import os.path\|from os.path\|os\.path\." lib/response_parser.py lib/fingerprint.py
```
</verification>

<success_criteria>
- response_parser extracts values via dot-path, auto-parses stringified JSON, strips markdown code blocks
- fingerprint produces deterministic SHA-256, saves/loads/compares with actionable error messages
- All tests pass (pytest exit code 0)
- No os.path usage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-libraries/01-02-SUMMARY.md`
</output>
