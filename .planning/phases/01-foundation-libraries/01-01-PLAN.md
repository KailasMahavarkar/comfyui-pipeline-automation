---
phase: 01-foundation-libraries
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/__init__.py
  - lib/naming.py
  - lib/sidecar.py
  - lib/manifest.py
  - tests/test_naming.py
  - tests/test_sidecar.py
  - tests/test_manifest.py
  - requirements.txt
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-04
  - LIB-05
  - LIB-15

must_haves:
  truths:
    - "Naming module resolves all 8 template tokens into valid filenames"
    - "Naming module supports Simple, Detailed, Minimal, and Custom preset templates"
    - "Sidecar module writes JSON file alongside an output path with full categorized tags and provenance"
    - "Manifest module appends rows to CSV with thread-safe locking and auto-creates header on first write"
    - "All modules use pathlib.Path exclusively for filesystem operations"
  artifacts:
    - path: "lib/__init__.py"
      provides: "Package init with public exports"
      contains: "from .naming import"
    - path: "lib/naming.py"
      provides: "Template token resolution and preset mapping"
      exports: ["resolve_template", "PRESETS"]
      min_lines: 50
    - path: "lib/sidecar.py"
      provides: "JSON sidecar writer"
      exports: ["write_sidecar"]
      min_lines: 15
    - path: "lib/manifest.py"
      provides: "Thread-safe CSV manifest appender"
      exports: ["append_manifest"]
      min_lines: 25
    - path: "tests/test_naming.py"
      provides: "Naming module tests"
      min_lines: 40
    - path: "tests/test_sidecar.py"
      provides: "Sidecar module tests"
      min_lines: 20
    - path: "tests/test_manifest.py"
      provides: "Manifest module tests"
      min_lines: 30
  key_links:
    - from: "lib/naming.py"
      to: "pathlib.Path"
      via: "_sanitize_filename returns safe string"
      pattern: "from pathlib import Path"
    - from: "lib/manifest.py"
      to: "filelock.FileLock"
      via: "lock file alongside CSV"
      pattern: "FileLock"
---

<objective>
Create the three simplest foundation library modules: naming (template token resolution), sidecar (JSON writer), and manifest (thread-safe CSV appender). These are stateless utility modules with well-defined I/O, perfect for TDD.

Purpose: Establishes the lib/ package structure and delivers three core modules that Save As node will depend on in Phase 2.
Output: lib/naming.py, lib/sidecar.py, lib/manifest.py with full test suites, plus lib/__init__.py and requirements.txt.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-libraries/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/ package, requirements.txt, and naming module with TDD</name>
  <files>
    lib/__init__.py
    lib/naming.py
    tests/__init__.py
    tests/test_naming.py
    requirements.txt
  </files>
  <action>
1. Create `requirements.txt` with: Pillow>=10.0, piexif==1.1.3, filelock>=3.13

2. Create `lib/__init__.py` as empty package init (will add exports incrementally).

3. Create `tests/__init__.py` as empty file.

4. RED: Create `tests/test_naming.py` with failing tests covering:
   - All 8 template tokens resolve correctly: {prefix}, {topic}, {date}, {time}, {resolution}, {counter}, {batch}, {format}
   - {datetime} token also resolves (research shows it in the pattern)
   - {width} and {height} tokens resolve individually
   - Preset templates: "Simple" -> "{prefix}_{date}_{time}", "Detailed" -> "{prefix}_{topic}_{resolution}_{counter}", "Minimal" -> "{prefix}_{counter}"
   - Custom template string works
   - Filename sanitization: forbidden chars (<>:"/\|?*) replaced with underscore
   - Filename sanitization: leading/trailing dots and spaces stripped
   - Filename length limited to 200 chars
   - Counter zero-padded to 4 digits (e.g., 0001)
   - Topic with special characters produces safe filename
   - All pathlib usage (no os.path imports in module)

5. GREEN: Create `lib/naming.py` implementing:
   - `PRESETS` dict mapping "Simple", "Detailed", "Minimal" to template strings
   - `resolve_template(template, prefix="comfyui", topic="unknown", resolution="unknown", width=0, height=0, counter=0, batch=0, format_ext="png")` -> str
   - Uses `str.replace()` for token substitution (not string.Template per research)
   - `_sanitize(text)` for topic/general text sanitization
   - `_sanitize_filename(filename)` for full filename sanitization (forbidden chars, length limit, strip dots/spaces)
   - `datetime.now()` for {date}, {time}, {datetime} tokens
   - {resolution} auto-derives from width/height if both provided, falls back to resolution param
   - Uses pathlib import, no os.path anywhere

6. Run tests, all must pass. Refactor if needed.

7. Update `lib/__init__.py` to export: `from .naming import resolve_template, PRESETS`
  </action>
  <verify>
    <automated>cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_naming.py -v</automated>
  </verify>
  <done>resolve_template handles all 8+ tokens, 4 presets work, filenames are sanitized for cross-platform safety, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create sidecar and manifest modules with TDD</name>
  <files>
    lib/sidecar.py
    lib/manifest.py
    tests/test_sidecar.py
    tests/test_manifest.py
    lib/__init__.py
  </files>
  <action>
1. RED: Create `tests/test_sidecar.py` with failing tests:
   - write_sidecar creates .json file alongside a given filepath (e.g., "output/img.png" -> "output/img.json")
   - JSON contains all passed metadata keys (prompt, negative_prompt, tags dict, tag_sources dict, pipeline dict, file dict)
   - File is valid JSON (json.load succeeds)
   - Creates parent directories if they don't exist
   - Uses pathlib.Path exclusively
   - encoding is utf-8

2. GREEN: Create `lib/sidecar.py` implementing:
   - `write_sidecar(filepath: Path, metadata: dict) -> Path` - writes JSON sidecar, returns sidecar path
   - Sidecar path = same name as filepath but with .json extension
   - `json.dump()` with indent=2 for readability
   - `encoding="utf-8"` on file open
   - `filepath.parent.mkdir(parents=True, exist_ok=True)` before writing

3. RED: Create `tests/test_manifest.py` with failing tests:
   - append_manifest creates CSV with header row on first call
   - append_manifest appends row without duplicating header on subsequent calls
   - Headers are: topic, resolution, variant_index, filename, path, tags, saved_at
   - Thread safety: concurrent appends from multiple threads don't corrupt file (use threading.Thread test with 10+ concurrent writes)
   - Lock file (.csv.lock) is created alongside CSV
   - Creates parent directories if needed
   - Uses pathlib.Path exclusively

4. GREEN: Create `lib/manifest.py` implementing:
   - `append_manifest(manifest_path: Path, row: dict) -> None`
   - Uses `filelock.FileLock` with lock_path = manifest_path.with_suffix(".csv.lock")
   - Uses `csv.DictWriter` with fieldnames = ["topic", "resolution", "variant_index", "filename", "path", "tags", "saved_at"]
   - Checks `manifest_path.exists()` to decide whether to write header
   - `encoding="utf-8"`, `newline=""` on file open

5. Run all tests, all must pass.

6. Update `lib/__init__.py` to also export: `from .sidecar import write_sidecar` and `from .manifest import append_manifest`
  </action>
  <verify>
    <automated>cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_sidecar.py tests/test_manifest.py -v</automated>
  </verify>
  <done>Sidecar writes valid JSON alongside output files, manifest appends CSV rows with thread-safe locking and auto-header creation, all tests pass</done>
</task>

</tasks>

<verification>
Run full test suite for all three modules:
```bash
cd C:/codingFiles/kai/comfyui-nodes && python -m pytest tests/test_naming.py tests/test_sidecar.py tests/test_manifest.py -v
```
Verify no os.path imports in any lib module:
```bash
grep -r "import os.path\|from os.path\|os\.path\." lib/
```
Should return no results.
</verification>

<success_criteria>
- lib/naming.py resolves all 8 template tokens and supports 4 presets with filename sanitization
- lib/sidecar.py writes valid JSON sidecars alongside output files
- lib/manifest.py appends to CSV with thread-safe locking and auto-header
- All tests pass (pytest exit code 0)
- No os.path usage in any lib/ module
- requirements.txt lists Pillow, piexif, filelock
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-libraries/01-01-SUMMARY.md`
</output>
